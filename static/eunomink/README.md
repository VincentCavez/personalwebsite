# Pen + Touch Interaction with Spreadsheets


## Install

In the main directory (code):

npm install

## Run

Launch a local HTTP server in the main directory (code)

Access the Web app with localhost:port/index.html
